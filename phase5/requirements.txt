# Subset minimal untuk menjalankan dashboard saja (app.py membaca CSV di csv/,
# tidak memerlukan umap-learn/numba/mlxtend). Untuk menjalankan Phase 1-4,
# pakai requirements.txt di root proyek.
dash>=2.17
plotly>=5.22
pandas>=2.0
numpy>=1.24
